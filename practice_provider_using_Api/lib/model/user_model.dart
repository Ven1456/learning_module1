class UserModel
{
final int userid,id;
final String title;
final bool completed;

  UserModel({
    required this.userid,
    required this.id,
    required this.title,
    required this.completed});
}